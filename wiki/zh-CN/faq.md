# 常见问题

[Wiki 目录](../README.md) · [English](../en/faq.md) · [技术文档](../../docs/zh-CN/README.md)

## 显示“请求已受理”，为什么没有短信？

页面对匹配和不匹配的信息显示相同结果，避免暴露人员资料。请核对员工号和 11 位登记手机号，再请管理员检查人员是否启用、短信服务是否启用，以及通知发送状态。请求会按来源地址、工号和手机号分别限流；已有未过期链接时，重复请求不会重新发送一条链接。已经绑定密码、动态密码或 FIDO2 的成员不会把短信当作找回或降级入口。

## 链接过期或已使用，怎么办？

链接只能成功兑换一次。默认有效期和服务端上限都是 10 分钟，实际仍受当前安全策略约束。已经登录的浏览器可继续使用有效会话；没有有效会话时，返回身份页面重新申请链接。

## 什么是三个认证目标？

初始化和系统设置中的增强认证要求管理员和成员使用密码、TOTP 与 FIDO2；组合认证要求双方使用密码和 TOTP；便捷认证（推荐）让管理员使用密码和 TOTP、成员使用短信一次性链接。目标名称只描述身份鉴别配置，不代表整个 ASVS 或等保条款已经验收通过。详见[安全与认证](security.md)。

## 忘记管理员密码怎么办？

让有权限的管理员从账号管理发起恢复任务。目标管理员需要在 30 分钟内打开 `/admin/password-reset` 设置新密码；已有 TOTP 时还需输入当前验证码，没有已验证 TOTP 时则必须在目标账号仍保持登录的浏览器中完成恢复。完成后该账号的其他会话会失效。系统当前不会通过邮件或短信发送恢复链接。

## AI 没有识别出日期，还能提交吗？

可以手工填写并核对字段。上传凭证且字段满足规则后可以提交；页面出现确认提示时按提示处理。AI 不会自动批准申请，管理员仍需核对材料。

## 为什么到期日无法随意填写？

资质可能采用固定月数规则，由签发日期或训练日期计算到期日。长期有效资质则不允许填写到期日。如果配置不符合业务要求，请联系管理员核对资质规则。

## 提交成功后，为什么资质日期还没变？

提交成功表示系统收到了更新申请。管理员审核通过后，正式记录才会更新。打开回执查看状态；若已退回，按退回原因补充材料并重新提交。

## 能上传 PDF 或 iPhone 的 HEIC 图片吗？

当前凭证上传接受图片，不接受 PDF。支持 JPEG、PNG、WebP、AVIF 和 GIF。HEIC/HEIF 依赖浏览器解码，Chrome 需要先转换；Safari 无法读取时也应转为 JPEG 或 PNG。

## 为什么管理员看不到某个成员或没有操作按钮？

访问范围受角色和所属单位限制。非超级管理员需要分配单位；审核员可以做审核决定，查看者不能写入数据。请让超级管理员核对账户角色、单位和启用状态。

## 为什么英文页面仍有中文资质名称？

界面语言与组织填写的业务数据是两类内容。检查该职位、资质或模板是否提供英文名称。切换界面语言不会自动翻译任意录入的名称和备注。

## 在哪里查看安装、升级和恢复方法？

阅读[技术文档](../../docs/zh-CN/README.md)。报告问题时提供部署版本、发生时间、相关页面、回执编号或请求编号，以及去除个人资料后的报错信息。不要附上访问链接、密码或验证码。

## 实现依据

[访问链接处理](../../src/app/api/pilot/access-link/route.ts)、[权限](../../src/server/admin-permissions.ts)、[有效期校验](../../src/lib/qualification-rules.ts)、[图片支持](../../src/lib/image-processing.ts)、[业务名称本地化](../../src/lib/i18n.ts)。
