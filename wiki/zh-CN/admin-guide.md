# 管理员指南

[Wiki 目录](../README.md) · [English](../en/admin-guide.md) · [技术文档](../../docs/zh-CN/README.md)

## 登录与权限

访问 `/admin/login`，输入管理员邮箱，并按登录页面要求填写密码和/或动态验证码（TOTP）。增强认证还要求 FIDO2 硬件验证器。实际登录方式由安全策略决定。

| 角色                     | 常见操作                                                                         |
| ------------------------ | -------------------------------------------------------------------------------- |
| 超级管理员 `SUPER_ADMIN` | 全部管理权限，包括管理员账户、安全策略、备份恢复、系统更新和审计。               |
| 管理员 `ADMIN`           | 维护人员、审核申请、管理日历和升级计划、重试通知，以及维护单位、职位和通知设置。 |
| 审核员 `REVIEWER`        | 查看人员和业务页面，并通过或退回申请。                                           |
| 查看者 `VIEWER`          | 查看业务页面，无审核决定和业务写入权限。                                         |

非超级管理员必须分配所属单位，人员数据按单位范围访问。能打开设置页面不表示能修改其中所有设置。系统至少需要保留一名启用的超级管理员。

认证目标、会话和安全审计的具体行为见[安全与认证](security.md)。首次设置或系统设置中有三个认证目标：增强认证要求管理员和成员使用密码、TOTP 与 FIDO2；组合认证要求双方使用密码和 TOTP；便捷认证让管理员使用密码和 TOTP、成员使用短信一次性链接。选择目标只配置身份鉴别方式，不等于整体标准已经通过。

系统设置的“安全与审计”区域可以查看活跃管理员会话、筛选近期审计记录，并查看公网扫描、撞库和分布式登录尝试的统计、趋势及采集完整性。每次登录后的摘要覆盖自上次成功登录以来的完整分钟，最长 30 天；首次登录显示最近 24 小时，同一会话只提示一次。具备 `settings.security.write` 权限的超级管理员可以结束其他会话；`audit.read` 只提供全局统计和审计读取权限，普通管理员只能看到本单位当前仍确认的完整关联账号，其他账号保持脱敏。切换认证目标、关闭高敏操作再次认证或变更重要安全策略时，系统会要求当前策略允许的再次认证，并使旧策略会话失效。

管理员密码不能由另一名管理员直接写入。账号管理发起 30 分钟有效的一次性恢复任务后，由目标管理员本人在 `/admin/password-reset` 设置至少 12 个字符的新密码。已有 TOTP 时还要输入当前验证码；没有已验证 TOTP 时，目标管理员必须在该账号仍保持登录的浏览器中完成恢复。完成后目标账号的其他会话会被撤销。

## 整理成员与资质要求

1. 在系统设置中检查组织、单位和职位配置。成员管理首页按已安装的职位模板展示入口。
2. 进入成员管理，选择职位，再查找或新增成员。有批量导入需求时，使用页面的 CSV 导入和预览流程，先处理预览中报告的错误。
3. 核对员工号、登记手机号、所属单位和启用状态。这些信息影响成员访问门户。
4. 在成员详情核对主职、兼任、职位要求和正式资质记录。
5. 在资质配置页面检查资质名称、有效期规则和字段限制，再让成员按规则提交更新。

## 审核更新申请

打开 `/admin/reviews`，选择待审核申请。

1. 查看凭证图片，核对提交字段、AI 辅助结果和当前正式记录。
2. 字段有误时使用人工纠正。原提交值保留在审核记录中，纠正后的字段标记为人工修正。
3. 材料满足要求时，选择通过，确认已核对凭证和信息；审核备注可选填。
4. 需要补充材料时，选择退回修改，填写具体原因，例如“请补传包含到期日期的证件下半页”。退回原因至少 5 个字符。
5. 检查审核结果和通知状态。外部通知按已配置的渠道发送。

若提示记录已被其他人修改，请重新加载申请并核对最新内容后再决定。

部分人员资质维护页面还允许有权限的管理员直接修正正式记录。保存会立即更新资质并写入审计记录，操作前应核实凭证和日期。

![资质审核工作台](../../docs/images/readme/qualification-review.png)

审核工作台示例，截图不包含真实人员信息。

## 日常跟进

仪表盘和成员列表用于定位缺失、过期或临期的资质。日历集中展示相关事项；升级计划页面用于维护阶段安排和进度。

通知日志用于查看发送状态，具备重试权限的管理员可重试适用的失败通知。成员未收到短信时，先核对人员资料，再检查通知配置和日志。

AI、对象存储、媒体优化、安全、备份和更新等系统设置由相应权限控制。部署、升级、备份与恢复的具体操作见[技术文档](../../docs/zh-CN/README.md)。

## 实现依据

[管理员权限](../../src/server/admin-permissions.ts)、[登录表单](../../src/app/admin/login/login-form.tsx)、[人员操作](../../src/components/admin/pilot-management-dialogs.tsx)、[审核对话框](../../src/components/admin/review-dialogs.tsx)、[系统设置](../../src/components/admin/admin-settings-view.tsx)。
