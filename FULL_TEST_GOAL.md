# CrewQual 全量测试持续目标（排除 AI 与真实通知）

## 可直接作为 Goal 使用的目标文本

在 `/root/crewqual` 中持续审计、补齐、执行并维护 CrewQual 的全量测试，覆盖当前仓库内除 AI 能力和真实通知外的全部产品功能、API、权限、安全边界、数据持久化、响应式界面及运行维护能力。不得因为现有测试全绿、单个主流程通过或完成一轮检查就停止；每轮从覆盖矩阵中选择尚未验证、证据不足或风险最高的项目继续推进，复现并修复已确认的范围内缺陷，同时补充防回归测试，直到本文的 Definition of Done 全部满足。测试必须确定、隔离、可重复，不使用真实个人数据，不调用 AI/OCR/VLM 服务，不向真实短信、飞书、Webhook 或其他通知目标发送任何内容。

## 1. 执行原则

1. 先阅读 `UI_IMPLEMENTATION_MASTER.md`、各批次说明、`CLAUDE.md`、`docs/backend-operations.md`、`package.json`、Prisma schema、全部生产路由和现有测试，动态建立功能清单；不得只照抄本文而遗漏之后新增的非 AI、非真实通知功能。
2. 在 `docs/testing/` 持续维护：
   - `coverage-matrix.md`：功能/路由/API/角色/视口/状态到测试证据的映射；
   - `test-runs.md`：环境、命令、结果、失败证据和复测结论；
   - `defects.md`：缺陷级别、复现步骤、根因、修复和回归测试。
3. 每个目标回合都先读取上述台账，按 `P0 → P1 → P2 → P3` 选择下一项；同一项没有新证据时不得反复做表面检查。
4. 发现确定缺陷后，在不新增产品范围、不改变已确认业务规则的前提下直接修复，并在最接近缺陷的层级增加回归测试。修复后先跑最小相关测试，再跑受影响测试集，最后进入全量回归。
5. 测试失败时保留 trace、截图、请求/响应摘要或最小日志；不得靠放宽断言、增加任意固定等待、跳过测试、降低并发安全或把错误吞掉来换取通过。
6. 需求级覆盖矩阵是“全量”的主判据，代码覆盖率只用于发现盲区，不能用一个总百分比代替功能、角色、状态和错误路径验收。
7. `typecheck` 与 `build` 必须串行运行，避免同时读写 `.next/types` 造成假失败。涉及共享数据库状态的远程 E2E 默认单 worker 串行。

## 2. 绝对排除项与安全护栏

### 2.1 不测试 AI 能力本身

以下内容不调用、不评估，也不以其成功作为任何用例前置条件：

- VLM/OCR/日期识别的模型调用、准确率、提示词、模型输出质量和供应商兼容性；
- `/api/evidence-images/[id]/recognitions`、`/api/recognitions/[id]` 的真实识别链路；
- recognition worker、`src/server/vlm.ts`、真实 VLM adapter 和 AI 设置连接测试；
- AI 日期候选、冲突、匹配或不匹配结果的正确性。

AI 字段可作为固定 fixture 中的不透明数据存在。仍必须验证：AI 禁用、繁忙、无结果或存在任意既有标记时，用户可继续手工填写和提交；AI 不得自动批准、自动退回、覆盖人工值或绕过权限。这是在验证人工业务边界，不是在测试 AI。

### 2.2 不发送真实通知

禁止访问真实短信、飞书、Webhook 或其他外部通知端点，禁止使用真实手机号、账号、token 和密钥。执行测试前必须确保：

- `SMS_ADAPTER=disabled` 或 `fake`；
- `FEISHU_ADAPTER=disabled`；
- `VLM_ADAPTER=disabled`；
- 测试环境没有可被误用的真实供应商 URL/密钥，必要时对非本机网络请求直接失败。

以下本地功能仍在范围内：业务事件生成通知记录、站内/Mock 状态、通知列表与详情、筛选、分页、统计、脱敏展示、attempt 历史、失败重试、通知路由与渠道配置的校验/加密/持久化/审计。重试只能走 disabled/fake adapter，并验证不会声称真实送达。飞行员访问链接通过测试 outbox、数据库或测试入口取得，不发送短信。

### 2.3 数据与环境安全

- 仅使用虚构、脱敏、确定性的测试数据；禁止复制生产数据。
- 数据库、对象存储 bucket/prefix、队列和浏览器存储必须与非测试环境隔离。
- 清理只能针对本轮生成且有明确命名空间的测试数据，不得执行宽泛删除。
- 固定时钟、时区 `Asia/Shanghai`、随机种子和 fixture；测试之间不得依赖执行顺序。
- `/dev/*` 只可用作确定性场景或诊断入口，不得把它们当作生产功能已经通过的替代证据。

## 3. 全量功能覆盖矩阵

下列每一项至少覆盖：正常路径、校验失败、空/不存在、读取失败、写入失败、重复提交、刷新恢复、权限拒绝及相关并发/版本冲突；不适用项要在矩阵中说明理由。

### A. 工程与运行基线

- Prettier、ESLint、TypeScript strict、Vitest、Next.js production build 全绿。
- 环境变量校验、Mock/remote 服务组合边界、错误响应格式和 request ID。
- Prisma generate、迁移、幂等 seed、应用启动、worker 启停、健康检查。
- Docker/Caddy 配置静态检查与测试环境启动冒烟；数据库、对象存储、队列健康状态正确显示。
- 备份与恢复演练、私有图片留存/过期清理、进程收到 SIGTERM/SIGINT 后正常退出。

### B. 身份、会话、权限与安全

- 管理员登录、TOTP、错误凭据、失败次数、锁定/解锁、退出、过期会话、撤销会话、Cookie 属性、CSRF、same-origin 和限流。
- 飞行员工号/手机号校验、一次性访问 token 领取、并发重放拒绝、过期/已使用 token、会话创建与过期；只使用 fake outbox，不发短信。
- `SUPER_ADMIN / ADMIN / REVIEWER / VIEWER` 的导航、页面、API 方法和操作级 RBAC；普通管理员单位隔离、无单位账号拒绝、跨单位 ID 猜测拒绝。
- 越权读取与写入、直接请求受保护 API、无效 JSON、超大输入、敏感错误泄露、开放重定向与基本注入/XSS 场景。
- 管理员口令/TOTP/集成密钥不明文持久化或回显；审计记录包含正确操作者、范围与结果但不泄露 secret。

### C. 飞行员端人工闭环

- `/pilot` 重定向、访问链接交换、身份页、会话恢复和未登录保护。
- `/pilot/qualifications` 六项核心资质名称、顺序、分组、有效/90 天/30 天/7 天/过期边界及上海日期跨月跨年计算。
- `/pilot/qualifications/[qualificationId]/update` 的手工填写、字段必填、日期顺序、等级/参数限制、草稿保存与刷新恢复、取消和重复提交保护。
- 图片类型、HEIC 浏览器差异、大小/尺寸限制、裁剪和 JPEG 处理、私有上传、签名 URL、无效/损坏/超大图片、孤立图片清理。不得触发 recognition API。
- `/pilot/submissions` 创建与 `/pilot/submissions/[id]` 回执、处理中/通过/退回等非 AI 业务状态、资源不存在和跨 Pilot 访问拒绝。
- AI 完全禁用时，合法手工资料与凭证仍能完成提交，且人工输入不被任何 fixture 中的 AI 字段覆盖。

### D. 管理员总览、飞行员与审核

- `/admin/dashboard` 统计、待审核、临期预警、本周升级节点以及跨页面数据变更后的同步。
- `/admin/pilots` 搜索、筛选、排序、分页、URL 恢复、空结果和大数据边界。
- 飞行员新增、编辑、软停用、重复员工号、单位/字段校验、乐观版本冲突。
- CSV 模板、动态资质列、BOM、引号/逗号、预览、部分无效行、重复行、批量导入幂等性与导入失败回滚。
- `/admin/pilots/[pilotId]` 基本资料、六项资质、升级轨迹、更新记录、电子档案与审计；无记录和不存在资源。
- 管理员直接编辑生效资质的字段校验、日期顺序、版本冲突和审计。
- `/admin/reviews` 搜索、非 AI 业务筛选、分页、URL 恢复、状态与导航待审数量同步。
- `/admin/reviews/[reviewId]` 脱敏凭证、提交值/当前值、人工纠正与撤销、退回原因、人工确认通过、二次决策拒绝、并发决策冲突、原记录保留、审计及生效资质同步。
- 任何已有 AI 标记只能作为只读提示，不测试其正确性；所有最终决定必须由有权限的人显式确认。

### E. 日历与升级计划

- `/admin/calendar` 的 90 天、月、周、人员时间线，日期导航、单位筛选、URL 深链与无效参数回退。
- 资质到期事件和升级节点的唯一派生，跨月/跨年/时区边界，选日人员资质清单与详情打开。
- `/admin/upgrade-plans` 搜索、类型/生命周期筛选、排序、分页、空/大数据与详情定位。
- `/admin/upgrade-plans/new` 桌面单页和移动三步流程、步骤 URL、草稿恢复、六节点固定顺序、整体日期/相邻日期校验、重复创建保护。
- 计划草稿、启动、暂停、恢复、取消、完成的合法状态机；取消原因、过期核心资质阻断、同一飞行员活动计划冲突和终态只读。
- 节点改期、无变化操作、登记完成、禁止跳节点/重复完成/暂停时完成、只推进相邻下一节点；计划详情、飞行员详情、日历和本地通知记录同步一致。
- 并发改期/状态变化的 optimistic version 冲突必须返回明确错误且不产生半写入。

### F. 资质配置

- `/admin/qualification-config` 六项核心项目的稳定 ID/编码/名称/顺序和补充项目边界。
- 配置深链、切换、无效 ID 回退、未保存修改确认、连续保存后的版本更新。
- 有效期规则判别联合、参数限制、两级提醒、检查项字段的输入校验；AI/OCR 检查项仅验证普通配置数据可保存，不调用或评估 AI。
- 新增补充项、重复编码/名称、停用规则、核心项目不可误删/改 ID；保存不得重写既有飞行员生效记录或核心完成率。
- 权限、错误恢复、重复提交和并发版本冲突。

### G. 本地通知记录与设置（无真实发送）

- `/admin/notifications` 统计、搜索、类型/渠道/状态筛选、分页、URL 恢复、详情、手机号/目标脱敏、空/大数据/错误状态。
- 审核、计划、节点和资质提醒等业务事件只生成一次正确的本地记录；失败重试追加 attempt 历史而非覆盖，重复点击不造成重复发送任务。
- disabled/fake adapter 下 queued/sent-demo/failed 状态和文案准确，不宣称真实到达；测试期间断言没有外部网络请求。
- 通知渠道与路由设置的字段校验、启停、密钥掩码与加密、版本冲突、权限和审计；“连接测试”只可针对本地假服务或明确跳过真实调用。

### H. 系统设置（排除 AI 分区）

- `/admin/settings` 的组织单位、管理员账号与角色、通知路由、安全策略、会话、审计和系统健康分区。
- 单位新增/编辑/启停、唯一编码、关联数量和普通管理员范围限制。
- 管理员新增、启停、角色/单位调整、重置密码/TOTP、撤销会话、自操作保护和最后一个超级管理员保护（若业务规则存在）。
- 会话 TTL、访问链接 TTL、最大失败次数、锁定时间等边界校验与实际鉴权行为一致。
- 设置读取/保存失败保留输入，可重试；敏感字段永不回显；审计与权限准确。
- AI 设置页、VLM 连接测试和模型配置行为明确排除，不以其未覆盖阻塞目标完成。

### I. API、数据库、对象存储与队列一致性

- 对所有纳入范围的 `src/app/api/**/route.ts` 建立方法、鉴权、权限、输入 schema、成功状态码、错误码、资源不存在、冲突和副作用测试。
- 数据库唯一约束、关系约束、软停用、事务原子性、乐观版本、分页游标/总数、单位隔离和序列化日期一致性。
- 私有证据图片不可公开枚举，签名 URL 短时有效，文件元数据/sha256 正确，失败上传不留脏记录。
- pg-boss 中除 recognition 和真实外发之外的入队事务、幂等、重试、cleanup/reminder 扫描可测试部分；外部通知 worker 只允许 disabled/fake adapter。
- 服务层 Mock 与 remote 实现对同一公开契约给出兼容结果，页面和业务组件不得直接依赖具体 Mock/供应商实现。

## 4. 横向质量维度

### 4.1 响应式与跨浏览器

- 所有生产页面至少覆盖 `390×844` 和 `1440×1024`。
- 复杂管理页额外覆盖 `768×1024`、`1023×900`、`1024×900`、`1280×900`，验证 1023→1024 导航和布局完整切换。
- Chromium 与 WebKit 运行完整关键主链；Firefox 至少运行关键冒烟与高风险表单/弹窗，若环境暂不支持要记录缺口而非假定通过。
- 无 document 级横向溢出；固定导航、操作栏、Toast、Dialog/Drawer 与最后一个可操作元素不相交；390×600 低视口仍可完成关键操作。
- 生产构建下逐页检查 console error、hydration error、404 资源、失败请求和布局闪烁。

### 4.2 可访问性与交互

- 键盘可完成登录、搜索/筛选、分页、日历事件、表单、审核决定、计划状态操作、Dialog/Drawer/Tabs。
- 焦点进入、陷阱、Escape、关闭后恢复正确；唯一 h1、landmark、label/error 关联、表格名称/scope、图标按钮名称、live region、错误聚焦和可见焦点完整。
- 状态不只依赖颜色；基础 WCAG AA 对比度；`prefers-reduced-motion` 生效。
- Loading/Skeleton/空/错误/已有数据互斥，mutation 具有 `aria-busy` 并防双击；Toast 不遮挡操作且可关闭。

### 4.3 数据规模、可靠性与性能

- 确定性覆盖至少 100 名飞行员、100 条审核、100 个计划、200 条通知和跨月日历事件。
- 覆盖超长中文/编号、Unicode、空数据、损坏 sessionStorage、断网、超时、500、慢响应和重试。
- 关键列表筛选/分页/统计一致，无重复 key、分页越界或明显交互冻结；记录可重复的基准环境和阈值。
- 核心读接口与页面首个可交互时间建立宽松但明确的回归阈值；性能退化必须有证据，不追求脱离实际环境的绝对分数。

## 5. 推荐执行顺序

每一轮按风险选择，不要求机械重跑所有步骤；里程碑全量回归按以下顺序严格串行：

1. 功能/路由/API/角色盘点与覆盖矩阵更新。
2. 最小复现和相关单元/组件测试。
3. API 与服务集成测试。
4. Mock 模式端到端主链与异常链。
5. remote 模式 PostgreSQL + S3 + pg-boss 集成链，AI/真实通知适配器全部禁用或 fake。
6. 响应式、键盘、无障碍、跨浏览器和视觉回归。
7. 数据规模、并发、故障恢复、安全与运行维护演练。
8. 严格串行执行：

```bash
corepack pnpm format
corepack pnpm lint
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
corepack pnpm test:e2e
corepack pnpm test:e2e:remote
```

如环境缺少数据库、对象存储、浏览器或容器能力，先完成不依赖该能力的项目并记录精确阻塞条件；不得把“未运行”写成“通过”。

## 6. 缺陷优先级

- **P0**：权限/单位隔离绕过、敏感信息泄露、数据破坏、真实外发风险、核心流程完全不可用。
- **P1**：登录/提交/审批/计划状态/配置保存错误，事务半写入，关键响应式或键盘路径不可用。
- **P2**：异常状态、分页筛选、错误恢复、跨浏览器、可访问性或明显视觉问题。
- **P3**：低风险一致性、文案、轻微视觉和测试可维护性问题。

P0/P1 发现后优先闭环；存在未解决 P0/P1 时不得宣称阶段完成。

## 7. 每轮交付格式

每轮结束前更新台账，并输出：

1. 本轮覆盖的矩阵项和新增证据；
2. 新发现缺陷、级别、根因与是否已修复；
3. 新增/修改的测试及其防回归目的；
4. 实际运行命令、通过/失败/跳过数量；
5. 明确区分“通过”“失败”“未运行”“因排除项不适用”；
6. 下一轮最高优先级事项和任何真实阻塞。

## 8. Definition of Done

只有同时满足以下条件才可结束此持续目标：

- 动态功能清单中所有非 AI、非真实通知的生产页面、API、服务、角色和后台任务均已映射到测试证据；没有无说明空白项。
- 所有核心主链和关键异常链在 Mock 模式通过；remote 模式覆盖真实 PostgreSQL、私有对象存储和队列边界。
- 所有纳入范围的 API 已验证鉴权、权限、输入、错误、冲突和副作用；单位隔离没有已知缺口。
- 目标视口、关键浏览器、键盘流程、基础无障碍、长/空/大/错误数据均完成验收。
- 无未解决 P0/P1；P2/P3 均已修复或有用户接受的明确处置。
- 所有自动化测试相互隔离，可在干净环境重复执行；连续两次完整全量回归结果一致且全绿，无 flaky 重试掩盖。
- AI 与真实通知排除项在台账中明确标记，测试日志能证明执行期间未发生 AI 调用或真实外部通知请求。
- 最终报告包含覆盖矩阵、环境、命令、结果、剩余风险和复现证据，且不存在把未运行项目描述为已通过的情况。

完成后，本目标文本仍可作为周期性回归基线：每次业务、路由、schema、权限、依赖或部署配置变化时重新盘点增量，新增覆盖后再执行受影响测试与全量回归。
